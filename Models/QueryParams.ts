export interface QueryParams {
    sourceExchangeName?: string,
    destNetwork?: string,
    lockAddress?: boolean,
    lockNetwork?: boolean,
    destAddress?: string,
    addressSource?: string,
    coinbase_redirect?: string,
    asset?: string,
}