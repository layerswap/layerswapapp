# Controlled Faro fixtures

`faro-dashboard-types-verification.json` summarizes the September 9 type-first dashboard **query/API checks**, not a browser capture or newly ingested telemetry. It records 34 raw errors across six type rows, exact count/session/latest-time reconciliation, hidden-variant drilldown, empty states, successful dashboard readback and the observed 1,000-row session timeline limit. No raw messages, session/wallet IDs or credentials are included. Browser rendering and clicks remain pending; these small development counts are not production baselines. See [type-first dashboard report](../faro-dashboard-types.md).

`faro-dashboard-clarity-verification.json` is a sanitized **query-test report**, not an outgoing payload or new ingestion fixture. It records 19 panel query/Grafana-dataframe cases, 23 exact stored-record drilldown checks and a real console-message comparison for the shared version-4 dashboard. Counts describe only the bounded controlled test windows. No raw messages, wallet/session IDs or credentials are included. Browser rendering/navigation remains unverified for this update. See [Checkpoint C](../faro-checkpoint-c.md).

`faro-optimized-policy-loki-observed.json` records the optimized-build console test queried at 15:36:30 UTC: one warning log and one console-error exception, despite two calls each in the supplied snippet; no debug/log/info marker records. Both records share one session. The nearby 86-record session window includes navigation, measurements and fetch/XHR mirrors, but no resource-performance events. This is bounded stored-only evidence, not outgoing-body capture, verification of specific Web Vitals, or source-map resolution. No TRACE call was tested. Marker text is synthetic and safe; the session ID is replaced with a fixture-only alias.

`faro-flow-close-loki-observed.json` verifies the user-operated testnet modal close at 15:18:20.44 UTC on 2026-09-08. The closing lifecycle record retains event/session swap and journey IDs. All 43 later-client-time records in the bounded query lack session swap/journey IDs and retain exactly the same session ID and wallet JSON. Two sanitized after-records accompany the closing exemplar. This is stored-only evidence, not outgoing capture or proof that every swap-owned field was absent. `outcome=abandoned` describes leaving the UI, not backend swap cancellation.

`faro-wallet-browser-payload.json` contains a complete user-saved collect request body (one console log at 2026-09-08 15:05:53.654 UTC), sanitized without reconstructing missing signals. Its wallet JSON, session/journey IDs, app scope and page URL were compared in memory with one exact client-timestamp/message/session match in dev Loki. All nonempty session attributes matched; outgoing `swap_id: ""` was absent as `session_attr_swap_id` in the parsed stored record. This supplies paired outgoing wallet evidence, superseding earlier outgoing-wallet-pending notes for the connected ready-state exemplar only. It has no spans or headers and does not verify startup restoration, flow exit, URL queries, deployed console/resource filtering, deduplication or source maps. The Desktop original was left unchanged and is not a shareable sanitized artifact.

`faro-safety-startup-loki-observed.json` records the preceding source-local startup and partial wallet-restoration state in dev Loki. It is stored-only evidence and a new-session start, not proof of clearing persisted swap context.

`faro-wallet-reconnect-loki-observed.json` records the later different-account reconnection. The current address differs; the Faro session and application/environment/version match earlier exemplars. Both addresses successfully locate historical records via exact first-element lookup. The journey ID differs and its cause is not established. This is not in-place account-switch or outgoing-body evidence.

`faro-wallet-loki-observed.json` contains three sanitized stored projections from the new wallet-context test. It verifies connected-wallet JSON, count/state fields and a first-element exact-address query in dev Loki, not a paired outgoing browser body. Address/session aliases preserve equality. The discovery query reached its 200-row limit; those rows are not a volume estimate or complete history.

`faro-wallet-disconnect-loki-observed.json` adds a later empty snapshot after the user disconnected. Original session/journey IDs were compared in memory with the prior connected exemplar; saved equality checks confirm preservation. The newest sampled record has `[]`, count `0`, state `ready`. The query returned its 30-row limit, including 12 empty snapshots. This is stored evidence, not a complete disconnect payload or proof of removal from historical/swap-form records. Account-change and multiple-wallet verification remain pending.

These fixtures were derived from actual Chrome Faro requests and stored Loki responses during the 2026-09-07/08 verification. See [Checkpoint B](../faro-checkpoint-b.md) for provenance, counts, exact query templates and limitations.

`faro-controlled-browser-payload.json` wraps six filtered transport bodies in `requests[].body`. The wrapper and scenario names are fixture metadata, not emitted Faro fields. Three HTTP OTLP spans are selected by their captured trace IDs, preserving linkage to their tracing mirrors. The malformed `[Maximum depth reached]` attributes are observed integration output and deliberately retained as evidence. This file must not be used as an example of valid OTLP attributes.

`faro-loki-observed-records.json` contains parsed-field projections from saved Loki query responses. `parsedFields` come from `| logfmt`; they are not ingestion labels. Raw lines are omitted to avoid retaining arbitrary messages or financial context. The five stored lifecycle exemplars are the swap-ID-filtered subset. The separate live session/journey query returned ten records.

`faro-sanitizer-browser-span-excerpt.json` (version 2) contains fresh user-provided DevTools excerpts after the sanitizer correction: one span's IDs and six typed attributes, plus the subsequently copied `browser.brands` resource attribute with three intact string entries. This is not a complete request body. It includes a separately queried, sanitized dev Loki mirror projection with exact original-ID equality checks; that query does not verify storage of `browser.brands`. No missing resource/scope metadata, span name/timing/status, events, links or headers are reconstructed. Unit tests combine the excerpts in an explicitly test-only minimal wrapper, not an independently captured enclosing association. Full URL query removal and identifier replacement are fixture sanitization, not proof of runtime URL normalization. Tempo remains unverified.

Sanitization replaces session, journey, swap, address, transaction, navigation, measurement, trace and span identifiers where present; trace/span placeholders retain valid nonzero hexadecimal forms and cross-record equality. URLs, browser user agents, stack-frame details and free-form reason/amount values are replaced. Original absent/empty values remain absent/empty, avoiding invented presence. Controlled error marker text and bounded enum values are retained. The placeholders do not locate live Grafana records.

The replay to the dev receiver retained original IDs and used different page/view replacements. These fixtures preserve the original browser schema after sanitization, not replay byte identity. The user-operated Sepolia swap has stored evidence only. No wallet-connection cancellation, gasless-signature rejection, production source-map resolution or Tempo ingestion is demonstrated here.

Regression checks validate preserved session/trace equality, optional view metadata, numeric-string event attributes, numeric TTFB measurement values, absent headers, the historical sanitizer defect and the fresh valid span excerpt. A complete post-fix browser envelope remains to be captured. Do not treat synthetic durations/counts as thresholds or distributions.
