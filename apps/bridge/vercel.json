{
  "installCommand": "pnpm install",
  "buildCommand": "cd ../.. && pnpm build:packages && cd apps/bridge && pnpm build",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "bash -c 'cd ../.. && git diff --quiet HEAD^ HEAD -- apps/bridge packages || exit 1'"
}