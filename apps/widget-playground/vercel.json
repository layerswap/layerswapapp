{
  "installCommand": "pnpm install",
  "buildCommand": "cd ../.. && pnpm build:packages && cd apps/widget-playground && pnpm build",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "bash -c 'cd ../.. && git diff --quiet HEAD^ HEAD -- apps/widget-playground packages || exit 1'"
}