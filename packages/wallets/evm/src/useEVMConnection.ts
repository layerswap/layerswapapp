// Deprecated: kept as a re-export shim for backward compatibility with
// external consumers. New code should import `useEvmConnection` from
// `./service/useEvmConnection`. This shim will be removed in a future release.
export { useEvmConnection as default } from './service/useEvmConnection'
