// Deprecated: kept as a re-export shim for backward compatibility with
// external consumers. New code should import `useFuelConnection` from
// `./service/useFuelConnection`. This shim will be removed in a future release.
export { useFuelConnection as default } from './service/useFuelConnection'
