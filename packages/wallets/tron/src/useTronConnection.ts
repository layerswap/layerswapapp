// Deprecated: kept as a re-export shim for backward compatibility with
// external consumers. New code should import `useTronConnection` from
// `./service/useTronConnection`. This shim will be removed in a future release.
export { useTronConnection as default } from './service/useTronConnection'
