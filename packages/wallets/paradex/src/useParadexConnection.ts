// Deprecated: kept as a re-export shim for backward compatibility with
// external consumers. New code should import `useParadexConnection` from
// `./service/useParadexConnection`. This shim will be removed in a future release.
export { useParadexConnection } from './service/useParadexConnection'
